#ifndef BUFFERS_H
	#define BUFFERS_H


	/* Server
		R/W disk
		Listens to clients
	Client
		R/W file
		If given a file, then start server

	Message recieved flag
	Queue holding function
	Send ack for every recieved */

	/*
		Share buffer memory between processes
		Everyone R/W same memory
	*/
#endif
